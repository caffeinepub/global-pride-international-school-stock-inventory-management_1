{
  "frontend/src/App.tsx": "Main app with TanStack Router and React-based auth guards — uses `useEffect`+`useNavigate` in guard components (AuthGuard, LoginGuard, IndexRedirect) to synchronously check localStorage on every render and redirect without any `beforeLoad` timing issues; includes Toaster for toast notifications.",
  "frontend/src/hooks/useAuth.ts": "Lightweight auth hook providing login/logout/isAuthenticated functions using localStorage session flag (key: gpis_session) with hardcoded credentials.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — `useCreateBill` now invalidates all `[\"bills\"]` queries broadly (not just the specific date) so the Dashboard's `useGetTotals` today-bills query is always refreshed after a bill is created; `useGetTotals` sets `staleTime: 0` on both sub-queries to ensure they always refetch when invalidated or the Dashboard remounts.",
  "frontend/src/components/Login.tsx": "Login page with username/password form, hardcoded credential validation via useAuth hook, inline error display, and redirect to /dashboard on success — no sign-up option.",
  "frontend/src/components/Layout.tsx": "Authenticated layout with collapsible dark sidebar navigation (Dashboard, Inventory, Billing, Reports), top header bar, and a logout button that clears the session flag and redirects to /login.",
  "frontend/src/components/SummaryCard.tsx": "Reusable metric card component displaying a title, value, icon, and optional subtitle with loading skeleton state.",
  "frontend/src/pages/Dashboard.tsx": "Dashboard page showing four summary cards — the \"Stock Sold Today\" and \"Income Today\" cards now reflect IST-based totals computed from bills fetched for today's India date, with subtitle labels updated to indicate IST.",
  "frontend/src/components/EditItemModal.tsx": "Modal dialog for editing an existing inventory item's name, category, quantity, and price — passes quantity as a plain number (not BigInt) to match the useUpdateItem hook's expected type.",
  "frontend/src/components/UpdateStockModal.tsx": "Modal dialog for adding additional stock units to an existing inventory item — passes the new quantity as a plain number (not BigInt) to match the useUpdateItem hook's expected type.",
  "frontend/src/components/InventoryTable.tsx": "Table component displaying all inventory items with color-coded stock levels and Edit/Add Stock action buttons per row.",
  "frontend/src/pages/Inventory.tsx": "Inventory management page with a fully rewritten Add Item form using individual field error states, explicit actor readiness check, `mutateAsync` with manual `queryClient.invalidateQueries`, sonner toast notifications on success/error, and form reset after successful submission; also renders InventoryTable, EditItemModal, and UpdateStockModal.",
  "frontend/src/components/PrintableBill.tsx": "Print-only receipt component updated to accept and display `billNumber`, `date`, and `studentClass` props — shows school name, bill number prominently, date, time, student name, class, payment mode, itemized table, and grand total.",
  "frontend/src/components/BillLineItem.tsx": "Single billing line item component with item selector, quantity input, unit price display, subtotal calculation, and stock validation error.",
  "frontend/src/pages/Billing.tsx": "Billing page updated to use `getISTDateString()` from dateUtils for the default bill date and the `max` constraint on the date picker, ensuring bills are always dated against the correct India calendar date.",
  "frontend/src/components/ReportSummary.tsx": "Three-card summary component for reports showing grand total, total units sold, and total income for a selected date.",
  "frontend/src/components/SalesTable.tsx": "Table component displaying aggregated sales data (item name, quantity sold, amount earned) for a selected date.",
  "frontend/src/components/RemainingStockTable.tsx": "Table component showing current remaining stock per inventory item with color-coded low/zero stock indicators.",
  "frontend/src/pages/Reports.tsx": "Reports page updated to initialize `selectedDate` and the date picker's `max` attribute using `getISTDateString()` so the default view always shows today's IST date and sales data is fetched for the correct India calendar date.",
  "frontend/src/lib/constants.ts": "Shared constants file exporting the list of product categories used across inventory and billing forms.",
  "frontend/src/lib/utils.ts": "Utility file exporting the cn() class merging helper using clsx and tailwind-merge.",
  "frontend/src/index.css": "Global styles with OKLCH color system (forest green brand, warm neutral palette), custom fonts (Inter + Plus Jakarta Sans), print media rules for bill printing.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom fonts, brand/emerald/amber/rose color tokens mapped to OKLCH CSS variables, and card shadow utilities.",
  "frontend/src/lib/dateUtils.ts": "New utility module exporting `getISTDateString()` which returns today's date as a YYYY-MM-DD string calculated using India Standard Time (UTC+5:30) to avoid timezone mismatches around midnight."
}